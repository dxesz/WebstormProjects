if (n < 0) {
    alert(`Power ${n} is not supported`);
}
// backtick quotes ` allow to split the string into multiple lines
let str = `
  ECMA International's TC39 is a group of JavaScript developers,
  implementers, academics, and more, collaborating with the community
  to maintain and evolve the definition of JavaScript.
`;
if (
    id === 123 &&
    moonPhase === 'Waning Gibbous' &&
    zodiacSign === 'Libra'
) {
    letTheSorceryBegin();
}
function pow(x, n) {
    let result = 1;
    //              <--
    for (let i = 0; i < n; i++) {
        result *= x;
    }
    //              <--
    return result;
}
for (let i = 0; i < 10; i++) {
    if (cond) {
    }
}
for (let i = 0; i < 10; i++) {
    if (!cond) continue;
}

function pow(x, n) {
    if (n < 0) {
        alert("Negative 'n' not supported");
    } else {
        let result = 1;

        for (let i = 0; i < n; i++) {
            result *= x;
        }

        return result;
    }
}
function pow(x, n) {
    if (n < 0) {
        alert("Negative 'n' not supported");
        return;
    }

    let result = 1;

    for (let i = 0; i < n; i++) {
        result *= x;
    }

    return result;
}
// function declarations
function createElement() {
}

function setHandler(elem) {
}

function walkAround() {
}

// the code which uses them
let elem = createElement();
setHandler(elem);
walkAround();


// the code which uses the functions
let elem = createElement();
setHandler(elem);
walkAround();

// --- helper functions ---
function createElement() {
}

function setHandler(elem) {
}

function walkAround() {
}


// Bad style
// This code has almost no spaces, also indentations are not same, contents in for are not in new line,
// long line can be split, sometimes misses ;